class Colword:
    # def __init__(self, colour, word, birth, top, left, x_dir, y_dir, speed):
    def __init__(self, word, colour, birth, left, top):
        # String word
        self.word = word
        # RGB
        self.colour = colour
        # Time of birth
        self.birth = birth
        # Left pos
        self.left = left
        # Top pos
        self.top = top
        # # 60 FPS
        # self.x_rate = x_dir * speed / 60
        # self.y_rate = y_dir * speed / 60



